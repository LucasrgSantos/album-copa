export const environment = {
  production: true,
  // Hospedado no GitHub Pages (estático) — precisa da URL absoluta do backend público.
  // Backend exposto via túnel ngrok (cedar-petri-impulsive).
  apiBaseUrl: 'https://cedar-petri-impulsive.ngrok-free.dev/api/v1',
};
